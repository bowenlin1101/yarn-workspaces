{
  "name": "yarn-monorepo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "signalMarkup", 
    "restaurantAPI"
  ],
  "scripts": {    
    "runrestaurant": "yarn --cwd restaurantAPI start", 
    "buildrestaurant": "yarn --cwd restaurantAPI build",   
    "buildreact": "yarn --cwd signalMarkup build",  
    "runreact": "yarn --cwd signalMarkup start"
  }
}
